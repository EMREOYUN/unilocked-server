import { Router } from "express";
import BaseController from "./base-controller";

export default class InviteBetaController extends BaseController {
    listen(router: Router): void {
        
    }
}